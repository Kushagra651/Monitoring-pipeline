# =============================================================================
# requirements.txt — ML Pipeline Monitoring System
# Python 3.10+ required
#
# Pinned to minor versions for reproducibility.
# To install: pip install -r requirements.txt
# =============================================================================


# -----------------------------------------------------------------------------
# API layer — FastAPI + serving
# -----------------------------------------------------------------------------
fastapi==0.111.0               # Web framework — async, OpenAPI docs auto-generated
uvicorn[standard]==0.29.0      # ASGI server — runs FastAPI in production
pydantic==2.7.1                # Data validation — used in schemas.py (v2 syntax)
pydantic-settings==2.2.1       # .env / environment variable loading
python-multipart==0.0.9        # Required for FastAPI form/file uploads


# -----------------------------------------------------------------------------
# ML — training and inference
# -----------------------------------------------------------------------------
scikit-learn==1.4.2            # Model training — LogisticRegression, RandomForest, etc.
xgboost==2.0.3                 # Gradient boosting — primary model for Adult Income
pandas==2.2.2                  # DataFrame operations — feature engineering, ingestion
numpy==1.26.4                  # Numerical ops — underpins sklearn and pandas
joblib==1.4.0                  # Model serialization — sklearn uses this internally


# -----------------------------------------------------------------------------
# Experiment tracking + model registry
# -----------------------------------------------------------------------------
mlflow==2.12.2                 # Experiment tracking, model registry, artifact store
boto3==1.34.95                 # S3 artifact store backend (optional but recommended)


# -----------------------------------------------------------------------------
# Data validation
# -----------------------------------------------------------------------------
great-expectations==0.18.15    # Schema + statistical validation of incoming data


# -----------------------------------------------------------------------------
# Drift detection + monitoring reports
# -----------------------------------------------------------------------------
evidently==0.4.30              # Data drift, concept drift, model quality reports


# -----------------------------------------------------------------------------
# Database — PostgreSQL
# -----------------------------------------------------------------------------
psycopg2-binary==2.9.9         # PostgreSQL driver — binary version avoids build deps
sqlalchemy==2.0.30             # ORM + query builder — used by logger.py
alembic==1.13.1                # DB migrations — track schema changes like git


# -----------------------------------------------------------------------------
# Metrics — Prometheus
# -----------------------------------------------------------------------------
prometheus-client==0.20.0      # Exposes /metrics endpoint scraped by Prometheus


# -----------------------------------------------------------------------------
# Orchestration — Apache Airflow
# NOTE: Airflow manages its own dependency resolution and requires SQLAlchemy<2.0,
# which conflicts with the SQLAlchemy==2.0.30 used by the rest of this project.
# Install Airflow separately inside its own container (see Dockerfile.airflow).
# DO NOT install apache-airflow in the same venv as this requirements.txt.
# -----------------------------------------------------------------------------
# apache-airflow==2.9.1
# apache-airflow-providers-postgres==5.10.2

pyarrow==15.0.2

# -----------------------------------------------------------------------------
# Notifications — alerting
# -----------------------------------------------------------------------------
slack-sdk==3.27.1              # Slack notifications from notify.py
requests==2.32.3               # HTTP client — satisfies evidently>=2.32.0, mlflow<3, great-expectations>=2.20


# -----------------------------------------------------------------------------
# Testing
# -----------------------------------------------------------------------------
pytest==8.2.0                  # Test runner
pytest-asyncio==0.23.6         # Async test support for FastAPI endpoints
httpx==0.27.0                  # Test client for FastAPI (replaces requests in tests)
pytest-cov==5.0.0              # Coverage reports


# -----------------------------------------------------------------------------
# Dev utilities
# -----------------------------------------------------------------------------
python-dotenv==1.0.1           # Load .env files locally (pydantic-settings uses this)
black==24.4.2                  # Code formatter
ruff==0.4.4                    # Linter — replaces flake8 + isort combined
pre-commit==3.7.0              # Git hooks — runs black + ruff before every commit